[[source]]
url = "http://mirrors.aliyun.com/pypi/simple/"
verify_ssl = false
name = "pip_conf_index_global"

[packages]
flask = ">=1.1.1"
humanfriendly = ">=4.18"
marshmallow = ">=3.0.1"
pytest = ">=5.3.0"
requests = ">=2.22.0"
sqlalchemy = ">=1.3.7"

[dev-packages]

[requires]
python_version = "3.11"
